#include <unistd.h>  //read()
#include <stdio.h>
#include <string.h>

int	main(int ac, char **av)
{
	size_t	len;

	if (ac != 2)
		return (1);
	if (av[1][0] == '\0')
		return (1);
	len = strlen(av[1]);
	malloc(sizeof(len));
	while (av[1][i])
	{

	}
}

